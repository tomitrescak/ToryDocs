---
title: 'Text'
metaTitle: 'Catalogue of text handling components from a Vanilla catalogue'
metaDescription: 'Catalogue of components from a Vanilla catalogue for Tory.js'
icon: 'cogs'
---

import { VanillaProps, VanillaDocs } from '../../src/examples/vanilla';

Set of base components usefull to build your forms or websites. 
Please choose the component of your interest from the right menu.

### Installation

```bash
yarn add @toryjs/components-vanilla
```

<h2 className="ui header">
  <i className="cogs icon"></i>
  <div className="content">
    Components
  </div>
</h2>

### Text

Text can display either a static or bound value:

<VanillaDocs id="text" header="Text Example" />
<VanillaProps name="Text" />


### Text

Image <img /> field. The source vakue can be bound, please see the `Fetch` example in providers section.

<VanillaDocs id="image" header="Image" />
<VanillaProps name="Image" />

### Link

A classic html anchor. 

<VanillaDocs id="link" header="Link"  />
<VanillaProps name="Link" />


### Markdown

A markdown formatted text. Please see the famous [cheat sheet](https://github.com/adam-p/markdown-here/wiki/Markdown-Cheatsheet) for all Markdown options.

<VanillaDocs id="markdown" header="Markdown"  />
<VanillaProps name="Markdown" />

### Formula

Formula uses javascript to dynamically evaluate data in your dataset. You can access the 
dataset values by `this.fieldName` notation. In the example below, please modify the input valuesand observe how the calculation changes.
In the formula field you can use the full power of javascript.

<VanillaDocs id="formula" header="Formula Example"  />
<VanillaProps name="Formula" />

### Selector

Selector can be used to interactively set the value in your dataset. It listens to the onClick event
upon which it stores the value from `source` to `target`.

<VanillaDocs id="selector" header="Selector Example"  />
<VanillaProps name="LinkSelector" />



