---
title: 'Form'
metaTitle: 'Catalogue of form components from a Vanilla catalogue'
metaDescription: 'Catalogue of components from a Vanilla catalogue for Tory.js'
icon: 'cogs'
---

import { VanillaProps, VanillaDocs } from '../examples/vanilla';

Set of base components usefull to build your forms or websites. Please choose the component of your interest from the right menu.

### Installation

```bash
yarn add @toryjs/components-vanilla
```

<h2 className="ui header">
  <i className="cogs icon"></i>
  <div className="content">
    Components
  </div>
</h2>

### Input

Input component is a standard form component aimed for further styling. 

<VanillaDocs id={'input'} header="Example: Input" group="vanilla"/>

Start typing into the input above to see the validation error. 
The `minLength` validation is defined on the textValue field of the form dataset. 
The style of the Error message is defined by the `FormConfig.ErrorView` component. 

<VanillaProps name="Input" />

### Textarea

Textarea component is a standard form component aimed for further styling. 

<VanillaDocs id={'textarea'} header="Example: Textarea" group="vanilla"/>
<VanillaProps name="Textarea" />


### Checkbox

Checkbox component is a standard form component aimed for further styling. 
<VanillaDocs id={'checkbox'} header="Example: Checkbox" group="vanilla"/>
Try to enable or disable fields to trigger validations.

<VanillaProps name="Checkbox" />


### Radio

Radio component is a standard form component aimed for further styling. 
<VanillaDocs id={'radio'} header="Example: Radio" group="vanilla"/>
Try to enable or disable fields to trigger validations.

<VanillaProps name="Radio" />


### Dropdown

Dropdown / Select component is a standard form component aimed for further styling. 
Dropdown is a very versatile component which can obtain the list of options from a variety of sources:

* `Options` (bound): Explicitly specified list of options. If you are binding the options to a handler, make sure it is not an asynchronous handler.
* `Async Options`: Asynchronous handler, returning `Promise` of `Options`.
* `Schema Source`: From a schema item's enum (Enumerations).

The values in the dropdown can be filtered by a value from the dataset. To specify such cascading filter, you need to define two values:

* `Filter Source`: the name of the dataset field (e.g. `country` with value `es`)
* `Filter Column`: the name of the column from the list of options that will match the dataset field. 
  For example following option list will filter out only `{ text: 'Barcelona', value: 'bcn', country: 'es' }`:

    ```bash
    { text: 'Sydney', value: 'syd', country: 'au' },
    { text: 'Melbourne', value: 'mel', country: 'au' },
    { text: 'Bari', value: 'bari', country: 'it' },
    { text: 'Barcelona', value: 'bcn', country: 'es' },
    { text: 'Rozhanovce', value: 'roz', country: 'sk' }
    ```

  Check the examples below.

<VanillaDocs id={'dropdown'} header="Example: Dropdown" group="vanilla"/>
<VanillaProps name="Dropdown" />

### Button

Button executes a provided onClick handler. It can also be set to be a `submit`.

<VanillaDocs id={'button'} header="Button Example" group="vanilla"/>
<VanillaProps name="Button" />

### Form

Html Form is a standard Html form element which allows you to specify a handler for the `onSubmit` actions.

<VanillaDocs id={'htmlForm'} header="Html Form" group="vanilla"/>
<VanillaProps name="HtmlForm" />
